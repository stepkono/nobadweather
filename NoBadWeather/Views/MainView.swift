//
//  MainView.swift
//  NoBadWeather
//
//  Created by Stepan Konoplianko on 09.10.24.
//

import SwiftUI
import SceneKit

struct MainView: View {
    var body: some View {
        VStack {
            /*
            ModeSwitchView()
            Spacer()
            Text("Here is AI-generated weather \n forecast for your location.")
                .multilineTextAlignment(.center)
            Spacer()
            SceneView(scene: SCNScene(named: "BlackJacket.usdc"), options: [.autoenablesDefaultLighting, .allowsCameraControl])
                .frame(width: 400, height: 600)
            Spacer()
             */
        }
    }
}

#Preview {
    MainView()
}
